import MainView from '../../js/View/MainView';

describe('MainView', () => {
  describe('constructor', () => {
    it('должен быть функцией', () => {
      expect(typeof MainView).toEqual('function');
    });
  });
});
