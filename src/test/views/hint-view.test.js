import HintView from '../../js/views/hint-view';

describe('HintView', () => {

  describe('constructor', () => {
    it('должен быть функцией', () => {
      expect(typeof HintView).toEqual('function');
    })
  })

})
