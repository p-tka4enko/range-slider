import GridView from '../../js/views/grid-view';

describe('GridView', () => {
  describe('constructor', () => {
    it('должен быть функцией', () => {
      expect(typeof GridView).toEqual('function');
    });
  });
});
