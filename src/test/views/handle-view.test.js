import HandleView from '../../js/views/handle-view';

describe('HandleView', () => {

  describe('constructor', () => {
    it('должен быть функцией', () => {
      expect(typeof HandleView).toEqual('function');
    })
  })

})
